import 'package:flutter/material.dart';

class CheckEventPage extends StatelessWidget {
  const CheckEventPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
